#import pandas as pd
#import json

# Load your JSON file into a DataFrame
# Replace 'your_file.json' with the path to your JSON file
#df = pd.read_json('C:\\Users\\LENOVO\\Downloads\\New Compressed (zipped) Folder\\TNTR_trade_prodprl_sample_data_k.json')

# Convert the 'from' column to string if it's not already
dff['from'] = dff['from'].astype(str)

# Extract the date part and store it in a new column named 'date'
dff['date'] = dff['from'].apply(lambda x: x.split('T')[0])

# Save the updated DataFrame back to a JSON file
# Replace 'updated_file.json' with the desired output file name
#df.to_json('C:\\Users\\LENOVO\\Downloads\\New Compressed (zipped) Folder\\updated.json', orient='records', lines=True)

# Optionally, print to verify
dff.head()

